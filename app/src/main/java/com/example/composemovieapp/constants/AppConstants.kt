package com.example.composemovieapp.constants

object AppConstants {

    //Retrofit connection timeout
    const val CONNECT_TIMEOUT: Long = 60000 * 3

    //Retrofit read timeout
    const val READ_TIMEOUT: Long = 60000 * 3

    //Retrofit write timeout
    const val WRITE_TIMEOUT: Long = 60000 * 3

    //Api Version
    const val API_VERSION = 3
}